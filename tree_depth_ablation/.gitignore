# Generated per-config trees and summary caches (rebuildable, large)
trees/

# Per-config LATTICE eval outputs
output/

# Keep the aggregated result csv tracked — it's the ablation output.

__pycache__/
*.pyc
